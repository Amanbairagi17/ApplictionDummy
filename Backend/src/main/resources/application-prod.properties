# Prod profile
server.port=8080

# Set your production origins (no wildcards ideally)
app.cors.allowed-origins=https://yourdomain.com,https://www.yourdomain.com

# JWT - must be set from environment in production
jwt.secret=${}
jwt.expiration-ms=${}
